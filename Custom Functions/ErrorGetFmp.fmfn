/**
 * =====================================
 * ErrorGetFmp (  )
 *
 * RETURNS:
 *		(num) errorCode if $error contains an error of type fmp, otherwise null
 *
 * DEPENDENCIES:
 *		Custom Functions: #Get
 *
 * NOTES:
 *		Use after calling ErrorSaveFmp custom function
 *
 * HISTORY:
 *		CREATED on 2012-NOV-20 Daniel Smith dansmith65@gmail.com
 * =====================================
 */

If (
	#Get ( $error ; "errorType" ) = "fmp" ;
	#Get ( $error ; "errorCode" )
)