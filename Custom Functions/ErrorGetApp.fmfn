/**
 * =====================================
 * ErrorGetApp (  )
 *
 * RETURNS:
 *		(num) errorCode if $error contains an error of type app, otherwise null
 *
 * DEPENDENCIES:
 *		Custom Functions: #Get
 *
 * NOTES:
 *		Use after calling ErrorSaveApp custom function
 *
 * HISTORY:
 *		CREATED on 2012-NOV-20 Daniel Smith dansmith65@gmail.com
 * =====================================
 */

If (
	#Get ( $error ; "errorType" ) = "app" ;
	#Get ( $error ; "errorCode" )
)